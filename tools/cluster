#!/bin/bash

echo 'WIP: To be implemented.'
echo $@
echo $(whoami)
pip install -r requirements.txt --user ${whoami}
./tools/runparser.sh

labs_path=$(pwd)/build/training-labs/labs/
echo $labs_path

if [ "$1" = "kvm" ]; then
    cd $labs_path
    $(sudo ./st.py -b cluster -p kvm)
fi
